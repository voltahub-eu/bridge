aiohttp==3.9.5
fastapi==0.111.0
uvicorn==0.30.1
bcrypt==4.1.3
flask==3.0.3
pyserial==3.5
requests==2.31.0
solaredge-local==0.1.5
uplink-protobuf==0.1.0
# solaredge-local (via uplink-protobuf) laadt oud-gegenereerde _pb2.py-bestanden
# die met protobuf>=4 breken ("Descriptors cannot be created directly") — geen
# van beide libraries pint dit zelf, dus zonder deze pin trekt een schone
# install een incompatibele protobuf-versie binnen en is SolarEdge kapot
# zonder dat het opvalt. Zie hoofd-app requirements.txt/commit ef16271 voor
# dezelfde fix aan de backend-kant.
protobuf==3.20.3
pysma==0.7.5
goodwe==0.4.9
